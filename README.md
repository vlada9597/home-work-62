# Home-Work-62 – Node.js Express Server with PUG and EJS

## Опис проєкту

Цей проєкт демонструє базовий **Express сервер**, який використовує **PUG** та **EJS** для рендерингу шаблонів, а також обробляє статичні файли CSS. Сервер надає просту логіку для користувачів та статей.

---

## 📂 Структура проєкту

home-work-62/
│
├── app.js # Головний серверний файл
├── controllers/ # Логіка обробки запитів
│ ├── homeController.js
│ ├── usersController.js
│ └── articlesController.js
├── routes/ # Маршрутизація
│ ├── homeRoutes.js
│ ├── usersRoutes.js
│ └── articlesRoutes.js
├── views/ # Шаблони представлення
│ ├── pug/
│ │ ├── users.pug
│ │ └── userDetails.pug
│ └── ejs/
│ ├── articles.ejs
│ └── articleDetails.ejs
├── public/ # Статичні файли (CSS)
│ └── styles.css
├── package.json # Конфігурація проєкту
└── README.md

yaml
Копировать код

---

## 🚀 Встановлення та запуск

1. **Клонувати проєкт або перейти до папки**:
cd path/to/home-work-62

Встановити залежності:
npm install

Запуск сервера:
npm start

# або

node app.js

Відкрити браузер і перейти за адресою:
http://localhost:3000

🌐 Маршрути сервера
1. Головна сторінка
URL: /

Метод: GET

Опис: Повертає простий текст "Welcome to the Home Page!"

2. Користувачі (PUG)
URL: /users

Метод: GET

Опис: Відображає список користувачів через PUG-шаблон users.pug.

URL: /users/:userId

Метод: GET

Опис: Повертає деталі конкретного користувача за id через PUG-шаблон userDetails.pug.

Приклад: /users/1 → інформація про користувача Vlad

3. Статті (EJS)
URL: /articles

Метод: GET

Опис: Відображає список статей через EJS-шаблон articles.ejs.

URL: /articles/:articleId

Метод: GET

Опис: Повертає деталі конкретної статті через EJS-шаблон articleDetails.ejs.

Приклад: /articles/1 → деталі першої статті


🎨 Статичні файли

Папка: /public
Файл: styles.css
Опис: CSS для оформлення усіх сторінок. Підключається автоматично через Express middleware.

✅ Перевірка роботи сервера
1. Через браузер
Просто відкрий URL у браузері:

http://localhost:3000/
http://localhost:3000/users
http://localhost:3000/users/1
http://localhost:3000/articles
http://localhost:3000/articles/1
Якщо сторінки відображаються — сервер працює нормально.

2. Через Node.js скрипт
Створіть файл checkRoutes.js у корені проєкту:

import http from "http";

const urls = [
  "http://localhost:3000/",
  "http://localhost:3000/users",
  "http://localhost:3000/users/1",
  "http://localhost:3000/articles",
  "http://localhost:3000/articles/1"
];

urls.forEach(url => {
  http.get(url, res => {
    console.log(`${url} → ${res.statusCode} ${res.statusMessage}`);
    let data = "";
    res.on("data", chunk => data += chunk);
    res.on("end", () => {
      console.log(`Response length: ${data.length} characters\n`);
    });
  }).on("error", err => {
    console.log(`${url} → ERROR: ${err.message}\n`);
  });
});
Запуск:

node checkRoutes.js

200 OK → маршрут працює.

401 Unauthorized у PowerShell → не помилка сервера, а особливість Windows; відкрийте у браузері або Git Bash.

🛠️ Встановлені залежності
express — веб-фреймворк для Node.js

pug — шаблонізатор для сторінок користувачів

ejs — шаблонізатор для сторінок статей

-----------------

💡 Примітки
Сервер використовує ES Modules ("type": "module" у package.json).

Статичні файли підключаються через express.static.

Маршрути розділені на home, users, articles для чистої архітектури.
